# Database URL (SQLite by default)
DATABASE_URL=sqlite:///./traffic_annotations.db

# CORS origins (use * for development, restrict for production)
CORS_ORIGINS=*

# Optional: YOLO model (defaults to yolov8m.pt)
# YOLO_MODEL_NAME=yolov8m.pt

# Optional: Detection thresholds
# AUTO_DEFAULT_CONF_TH=0.35
# MIN_BOX_AREA=800
